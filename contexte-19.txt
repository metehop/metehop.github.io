CONTEXTE — App météo "Metehop" (HTML/JS single-file)
========================================================================
Fait suite à contexte-18.txt (ne le réécrit pas : les points encore
valides n'y sont que résumés avec renvoi). Session du 11/09/2026 en
PLUSIEURS volets : (1) nettoyage stations (ITENCI3 + Pioupiou seuls) ;
(2) recherche de sources amateurs compatibles ; (3) accélération
micro-climat ; (4) test live openSenseMap/sensor.community à Grenoble ;
(5) implémentation PUIS retrait de sensor.community (bloqué côté
utilisateur) ; (6) toggle TREC retiré, TREC toujours actif en dur.

FICHIER DE TRAVAIL : C:\Users\Famille Martineau\Documents\Baudouin\open code AI\Metehop\index.html
(~1,04 Mo après nettoyage ; footer 104 → 108).
Backup pré-travaux (état 104) : C:\Users\FAMILL~1\AppData\Local\Temp\opencode\index_backup.html.
Scripts de chirurgie (non versionnés, rejouables pour audit) : step1_cleanup.py,
step2_fix.py, step3_micro.py, step4_verify.py, step5_fixdecl.py, step6_decl.py,
step7_sc.py, step8_jina.py, step9b.py, step10_fix.py, step10_orphan.py, parse_sc.py.

========================================================
VERSIONING DU FOOTER
========================================================
104 (fin contexte-18) → **108** : 105 = nettoyage stations + micro-climat ;
106 = sensor.community ; 107 = repli Jina + filtre indoor ; 108 = retraits
sensor.community + toggle TREC. Date "11/09/2026" (date système du jour).

========================================================
1) NETTOYAGE STATIONS — gardés ITENCI3 + Pioupiou/OpenWindMap
========================================================
loadNearbyStation() (WU v2 / ITENCI3) intact. loadSynopStation RENOMMÉE
loadPioupiouStation (mêmes ids DOM synop-station-* conservés pour minimiser
les touches ; titre passé à "Pioupiou / OpenWindMap — vent réel", distance
arrondie à 1 décimale) ; synopStationLoaded → pioupiouStationLoaded (5 occ.).
SUPPRIMÉ (~159 Ko, 1 221 151 → 1 061 417 octets disque) : référentiel MF
embarqué 123 Ko (MF_STATIONS_EMBEDDED + parseMfStations/loadMfStationIndex/
findNearestMfStations), renderMfLiveObs + ligne token MF (saveMfToken,
get/show/hideMfToken, mfParseDate, mfPickObs), buildInfoclimatUrl +
slugifyStationName + lien externe Infoclimat, SYNOP/Opendatasoft
(extractSynopCoords, constantes SYNOP_*), repli Bright Sky (magnusHumidity,
localDayStr, stationPillsHTML avec lui). Meta description + featureList
JSON-LD mis à jour (MF → Pioupiou/OpenWindMap).

========================================================
2) RECHERCHE AMATEURS SANS CLÉ / SANS SERVEUR (compatibilité single-file)
========================================================
INCOMPATIBLES : Netatmo getpublicdata (OAuth2), Windy Stations (clé API),
Infoclimat API (compte + clé), WOW/PWSWeather (clés), WeatherCloud (API
privée + pas de CORS — déjà expertisé contexte-17). COMPATIBLES
techniquement, SIGNALÉS SANS IMPLÉMENTER (consigne) : openSenseMap (sans
clé, CORS * documenté) et sensor.community (sans clé, open data, CORS à
valider en navigateur).

========================================================
3) MICRO-CLIMAT ACCÉLÉRÉ (seuils et formules inchangés)
========================================================
fetchMicroClimateGeoSignals : cache mémoire + localStorage 30 j
(metehop_microgeo_v1, clé lat/lon arrondis ~100 m, borné à 60 lieux —
tout retour sur lieu connu est instantané) ; Overpass 3 → 2 requêtes
parallèles (bâti 700 m inchangé, seuils 30/120 intacts + requête fusionnée
eau 800 m / lieux peuplés 5 km, lieux inclus seulement si population
inconnue) ; timeouts Overpass 15 s → 10 s (pire cas parallèle ~10 s).
Relief (2 anneaux 2/6 km) inchangé. Helpers microGeoCacheGet/Set.

========================================================
4) TEST LIVE 11/09/2026 ~07h20 LOCALES (Grenoble 45.1885, 5.7245)
========================================================
- openSenseMap : PIÈGE — bbox = lon,lat,lon,lat (nos premiers tests vides
  cherchaient en Somalie). 14 boîtes zone élargie, 2 actives outdoor :
  ELSYS_EMS_PERRET_SEE/NEE (~1 km E, 22,5-22,6 °C / 55-56 %, douteux à 7h30,
  même propriétaire) ; reste mort (2020-2024) + 1 indoor à ~13 km.
  Valeurs via /boxes/{id} (lastMeasurement.value).
- sensor.community (filter/area=lat,lon,10, ~131 échantillons, 57 avec T,
  frais de quelques minutes) : #32173 16,2 °C (0,3 km), #91272 12,9 °C
  (1,6 km), #25650 14,7 °C (2,4 km), #54726 13,6 °C (2,4 km) plausibles ;
  21-24 °C = intérieur. DÉCOUVERTES API : location.indoor (0/1, confirme
  les suspects) et pressure_at_sealevel (Pa, déjà niveau mer).

========================================================
5) sensor.community IMPLÉMENTÉ PUIS RETIRÉ (footer 106-107, annulé en 108)
========================================================
Implémentation (annulée ensuite) : sous-section 3 (mêmes patterns que
ski-station-select), ≤10 km, 20 capteurs max triés par distance, label
"#id · type — dist — temp" + ★ présélection = PLUS PROCHE DU MODÈLE
Open-Meteo à l'heure courante (scModelRefTemp, généralise la consigne
"plus proche des 11,5 °C MF à Grenoble"), warning permanent + écart au
modèle, exclusion indoor=1, pression mer prioritaire (Pa→hPa), horodatage
UTC explicite (scFormatTsUtc), filtre <3 h, déduplication par capteur,
cache 5 min, câblage setGroup/fetchWeather (sensorCommunityLoaded).
BLOCAGE constaté par l'utilisateur même en https → diagnostic : CORS OK
PROUVÉ (Invoke-WebRequest -UseBasicParsing : Access-Control-Allow-Origin: *,
pas de CSP dans la page) → suspect n°1 = bloqueur de pub du poste (déjà
vu : geopf ERR_BLOCKED_BY_CLIENT). Repli r.jina.ai TESTÉ FONCTIONNEL
(relaie le JSON ; parser res.text + extraction crochets, timeout 15 s).
Retrait total sur consigne ("ne garder que ITENCI3 et Pioupiou") : HTML,
JS, câblage, JSON-LD — vérifié x0 partout. Si réutilisé un jour :
l'exclusion indoor et pressure_at_sealevel restent les points clés.

========================================================
6) TREC TOUJOURS ACTIF (toggle retiré)
========================================================
Checkbox #trec-toggle + onTrecToggleChange + clé metehop_trec_v1 + init
DOMContentLoaded supprimés ; `const metehopTrecEnabled = true;` en dur.
Le champ était déjà calculé dès que possible : priorité vitesse TREC,
plume courbe + suffixe [TREC] désormais systématiques hors neige.
metehopTrecActiveNow (grille vide → repli triangle/régression/steering)
et setEvoLoading inchangés. 6 commentaires mis à jour.

========================================================
BUGS DE SESSION (tous corrigés, causes notées pour la suite)
========================================================
- cut_span end EXCLUSIF : penser à réémettre ce qui doit survivre (un
  </div> en trop + un fragment "voir loadSensorCommunity" — détectés par
  le parseur HTML, backup comparé propre).
- fix_line remplaçant la ligne ENTIÈRE : avait mangé `let
  pioupiouStationLoaded` (assignations globales implicites) — restauré.
- Séquences \uXXXX dans les COMMENTAIRES JS restent visibles telles
  quelles (ne sont décodées que dans les chaînes) — tout passer en UTF-8
  réel dans les commentaires.
- Tiret cadratin (U+2014) corrompu en U+FFFD dans un script .py transitant
  par l'outil Write : dans les ancres/regex, préférer l'ASCII ; en cas de
  doute, remplacement par lignes (mot-clé ASCII) plutôt que bloc exact.
- Ancres multi-lignes : ne pas oublier l'indentation de la 2e ligne.
- Invoke-WebRequest SANS -UseBasicParsing → invite IE qui bloque en
  non-interactif ; AVEC -UseBasicParsing les en-têtes sont lisibles.
- `py -c` + accents dans la console cp1252 : passer par des fichiers
  (Write + Read) pour tout texte accentué.

========================================================
POINTS DE VIGILANCE POUR LA SUITE (cumulés)
========================================================
- **Pas de runtime JS ici (ni node, ni navigateur)** : vérification par
  grep + HTMLParser maison (0 erreur, 0 id dupliqué, 0 getElementById sans
  cible) + équilibrage accolades (3561/3561). Tests navigateur à
  l'utilisateur (footer -108, Ctrl+F5) : encart 3h avec [TREC] systématique
  + plume courbe ; carte stations = ITENCI3 + Pioupiou uniquement.
- **Compagnons devenus inutiles** (conservés sur disque) : stations-mf.json,
  liste-stations.csv, gen_stations.ps1, inject_stations.ps1, 2 swagger MF —
  à archiver/supprimer. Ne PAS déployer cle_API_meteofrance.txt si présent.
- **Si sensor.community revient un jour** : le blocage venait très
  probablement du bloqueur de pub (CORS prouvé OK) ; repli Jina + filtre
  indoor + pressure_at_sealevel déjà validés, à réimplémenter depuis ce
  contexte (code retiré, non conservé dans le fichier).
- Hérités (contexte-12 à 18) inchangés, dont fiabilité auto-mesurée
  (libellé après ≥3 vérifications) et micro-climat OFF par défaut.
